# This item is stored in the hand slot while it is blocked, to represent what is blocking that hand.
- type: entity
  id: VirtualItem
  name: ent-VirtualItem
  categories: [ HideSpawnMenu ]
  components:
  - type: Item
    size: Ginormous # no storage insertion visuals
  - type: VirtualItem
  - type: CMVirtualItem
